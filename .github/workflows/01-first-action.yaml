name: GitHub First Action

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  first-run:
    runs-on: ubuntu-latest

    steps:
      - name: 🌟 Welcome Message
        run: echo "🚀 Welcome to GitHub Actions!"

      - name: ✅ Verify First Job
        run: echo "🎉 First job completed successfully."

  second-run:
    runs-on: ubuntu-latest

    steps:
      - name: ⚙️ Execute Second Job
        run: |
          echo "🔧 Running the second job..."
          echo "✨ Workflow execution completed successfully!"
          exit 0
